# Anomaly detection in signals utilizing deep learning

The objective of this academic project was to build deep learning architectures such as auto-encoders to detect outliers in a signal dataset, utilizing reconstruction errors and clustering in the latent space.
